{%- unless proImageSize -%}
	{%- assign proImageSize = '800x' -%}
{%- endunless -%}

<section class="premium_product_section same__cols">
  <div class="section-wrap">
    {% for block in section.blocks %}
    {% assign collection = collections[block.settings.collection] %}
    <div class="collection_group" id="{% if block.settings.collection contains "port" %}port-assortment{% else %}parisiansummer-assortment{% endif %}">
      <div class="collection_group_head">
<!--         <div class="imgWrapper">
          <img class="lazyload" title="" alt="" width="360" height="240" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_1440_-_we-silk_-_tank-look.jpg" />
        </div> -->
        {% if block.settings.sub_title != blank %}
        <h4>{{ block.settings.sub_title }}</h4>
        {% endif %}
        {% if block.settings.title != blank %}
        <h2 class="text-center">{{ block.settings.title }}</h2>
        {% endif %}
        {% if block.settings.caption != blank %}
        <p>{{ block.settings.caption }}</p>
        {% endif %}
      </div>
      
    </div>
    {% endfor %}
  </div>
</section>

<section class="shopTheLookContainer ss-shop_the_look" id="shop_the_look">
<!--   <h2><span>Inner-Outerwear</span><br/>Go-to basics for your elevated summer-chic look</h2> -->
  <div class="shopTheLookWrapper firstLook">
    <article class="lookWrapper">
      <div class="lookWrapperCol imgWrapper">
        <img class="lazyload" title="" alt="" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_1440_-_we-silk_-_castlewall-collection.jpg?v=1659730777" width="" height="" />
      </div>
      <div class="lookWrapperCol prodWrapper premium_product_section">
        {% assign collection = collections["washable-eco-silk-shop-the-look-one"] %}
        <div class="product-grd collection_group fullwidth epcWrapper">
          <div class="products_cols collection_group_list ebyContainer pcs">
            {% for product in collection.products limit: block.settings.pdp_range %}
                  {%- assign hasFlag = "false" -%}
                  {% for prodTag in product.tags %}
                      {% if prodTag == "flag-new_arrival" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "New" %}
                      {% endif %}
                      {% if prodTag == "flag-on_sale" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "Sale" %}
                      {% endif %}
                  {% endfor %}
                  {% if product.type == "Silk Blouse" %}
                      {%- assign prodNameTitle = "blouse" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "blouseColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "blouseColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Tank" %}
                      {%- assign prodNameTitle = "tank" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "tankColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "tankColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Shorts" %}
                      {%- assign prodNameTitle = "shorts" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "shortsColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "shortsColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Scarf" %}
                      {%- assign prodNameTitle = "scarf" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "scarfColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "scarfColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Dress" %}
                      {%- assign prodNameTitle = "slip dress" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "dressColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "dressColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% endif %}

                  {% if prodNameTitle == "highwaisted_thong" %}
                      {% assign prdImgFileName = "hwthong" %}
                  {% else %}
                      {% assign prdImgFileName = prodNameTitle %}
                  {% endif %}

                  {% assign premImgFileName = prodColorTitle | replace: " ", "" | append: "---" | append: prdImgFileName | append: "---missy---tile_fr.jpg" %}

                  <div class="col velaProBlock quickadd carousel-item ept" data-size-guid="{% render 'collection-selectors-charts', product_target: product %}">
                    <div class="col-wrap velaProBlockInner">
                      {% if hasFlag == "true" %}
                      <div class="prodTileFlag">
                        <div class="prodTileFlagInnerWrapper">
                          <span>{{ prodFlag }}</span>
                        </div>
                      </div>
                      {% endif %}
                      <div class="product-image proHImage d-flex flexJustifyCenter">
                        <a class="proFeaturedImage" href="{{ product.url }}" draggable="false">
                          {% for image in product.images %}
                            {%- assign itemNumber = forloop.index -%}
                            {%- if itemNumber == 1 -%}
                              <img class="lazyload" width="240" height="300" draggable="false" alt="{{ image.alt }}" title="{{ image.alt }}" data-src="{{ image | img_url: 'master' }}">
                            {%- endif -%}
                          {% endfor %}
                          <div class="col-pro-details">
                            {% if product.type contains "Underwear" %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% else %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% endif %}
                            <!-- <div class="prodTileReviewWrapper">
                              {% if product.metafields.stamped.reviews_average %}
                              <span class="colReviewAggrWrapper" draggable="false"><span>{{ product.metafields.stamped.reviews_average | round:2 }}</span><i class="reviewStar"></i></span>
                              {% endif %}
                            </div> -->
                          </div>
                        </a>
                      </div>
                      <div class="product-details proButton clearfix">
                        {% if product.type == "Silk Scarf" %}
                            {%- assign variantCount = product.variants | size -%}
                            <form action="/cart/add" method="post" enctype="multipart/form-data" class="formAddToCart">            				  
                                <input type="hidden" name="id" value="{{ product.variants.first.id }}" />
                                {%- if sold_out -%}
                                    <button class="btn btnProduct btnAddToCart disabled" type="submit" value="Submit" disabled>
                                        <span class="icons icon-handbag"></span>
                                        <span class="text">{{ 'products.product.sold_out' | t }}</span>
                                    </button>
                                {%- else -%}
                                    {%- if variantCount > 1 -%}
                                        <a class="btn btnProduct btnAddToCart" href="{{ product.url }}" title="{{ 'products.product.select_options' | t }}">
                                            <span class="icons icon-handbag"></span>
                                            <span class="select_options text">{{ 'products.product.select_options' | t }}</span>
                                        </a>
                                    {%- else -%}
                                        <div class="quick_btn">
                                          <div class="productQuickAdd">
                                            <button class="btn  btnProduct btnAddToCart" type="submit" value="Submit">
                                              <div class="quickAddBtnInnerWrapper">
                                                <span class="text">Add to Cart</span>
                                                <div class="prodTilePriceWrapper{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price %} vipHighlight{%endif%}">
                                                  <p class="price bfx-price">{{ product.price | money_without_trailing_zeros  }}</p>
                                                  {% if customer.tags contains 'Active Subscriber' %}
                                                  {% if product.type == "Mask"%}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$6</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Core Bralette" or product.type == "Cotton Bralette" or product.type == "Mesh Bralette" %}
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                    {% else %}
                                                    <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                    {% endif %}
                                                    <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                      {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                                      {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                      {% comment %}
                                                      <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                      {% endcomment %}
                                                      <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                      {% else %}
                                                      <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                      {% endif %}
                                                    </div>
                                                  {% elsif product.type == "Wrap Top" or product.type contains "Silk" %}
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                    {% else %}
                                                    <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                    {% endif %}
                                                    <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                      {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                                      {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                      {% comment %}
                                                      <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                      {% endcomment %}
                                                      <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                      {% else %}
                                                      <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                      {% endif %}
                                                    </div>
                                                  {% elsif product.type == "Core Underwear" and product.tags contains "Featured Style" or product.type == "Mesh Underwear" %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$9</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Core Underwear" or product.type == "Cotton Underwear" or product.type == "Tank"  or product.type == "Bodysuit" or product.type == "Mesh Underwear" %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', '' }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 43.24 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$7.50</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type contains "Premium" %}
                
                                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                
                                                  {% elsif product.type contains "Surprise Panty Pack" %}
                
                                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                
                                                  {% endif %}
                
                                                  {% else %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    {%- assign discount = product.price | times: shop.metafields.ebyacf.sitewide_discount_price | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% endif %}                                
                                                </div>
                                              </div>
                                            </button>
                                          </div>
                                          <span class="quickAddPriceNote{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price != "" %} vipHighlight{%endif%}">subscriber price</span>
                                        </div>
                                    {%- endif -%}
                                {%- endif -%}
                            </form>
                        {% else %}
                            <div class="quick_btn">
                              <div class="productQuickAdd">
                                <button class="btn btnProduct btnProductQuickAdd" data-href="#velaQuickAdd-{{ product.id }}" data-handle="{{ product.handle }}" title="Quick Add +">
                                  <div class="quickAddBtnInnerWrapper">
                                    <span class="text">Add to Cart</span>
                                    <div class="prodTilePriceWrapper{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price %} vipHighlight{%endif%}">
                                      <p class="price bfx-price">{{ product.price | money_without_trailing_zeros  }}</p>
                                      {% if customer.tags contains 'Active Subscriber' %}
                                      {% if product.type == "Mask"%}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                        {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$6</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Core Bralette" or product.type == "Cotton Bralette" or product.type == "Mesh Bralette" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                        {% elsif product.type == "Wrap Top" or product.type contains "Silk" %}
                                          {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                          <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                          {% else %}
                                          <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                          {% endif %}
                                          <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                            {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                            {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                            {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                            {% comment %}
                                            <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                            {% endcomment %}
                                            <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                            {% else %}
                                            <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                            {% endif %}
                                          </div>
                                      {% elsif product.type == "Core Underwear" and product.tags contains "Featured Style" or product.type == "Mesh Underwear" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$9</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Core Underwear" or product.type == "Cotton Underwear" or product.type == "Tank"  or product.type == "Bodysuit" or product.type == "Mesh Underwear" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', '' }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 43.24 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$7.50</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type contains "Premium" %}
    
                                      <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
    
                                      {% elsif product.type contains "Surprise Panty Pack" %}
    
                                      <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
    
                                      {% endif %}
    
                                      {% else %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                        {%- assign discount = product.price | times: shop.metafields.ebyacf.sitewide_discount_price | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                        {% endif %}
                                      </div>
                                      {% endif %}                                
                                    </div>
                                  </div>
                                </button>
                                <div class="selection-wrapper" id="velaQuickAdd-{{ product.id }}">
                                </div>
                              </div>
                              <span class="quickAddPriceNote{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price != "" %} vipHighlight{%endif%}">subscriber price</span>
                            </div>
                          {% endif %}
                      </div>
                    </div>
                  </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </article>
  </div>
  <div class="shopTheLookWrapper secondLook">
    <article class="lookWrapper">
      <div class="lookWrapperCol imgWrapper">
        <img class="lazyload" title="" alt="" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_1440_-_we-silk_-_cactus-collection.jpg?" width="" height="" />
      </div>
      <div class="lookWrapperCol prodWrapper premium_product_section">
        {% assign collection = collections["washable-eco-silk-shop-the-look-two"] %}
        <div class="product-grd collection_group fullwidth epcWrapper">
          <div class="products_cols collection_group_list ebyContainer pcs">
            {% for product in collection.products limit: block.settings.pdp_range %}
                  {%- assign hasFlag = "false" -%}
                  {% for prodTag in product.tags %}
                      {% if prodTag == "flag-new_arrival" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "New" %}
                      {% endif %}
                      {% if prodTag == "flag-on_sale" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "Sale" %}
                      {% endif %}
                  {% endfor %}
                  {% if product.type == "Silk Blouse" %}
                      {%- assign prodNameTitle = "blouse" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "blouseColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "blouseColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Tank" %}
                      {%- assign prodNameTitle = "tank" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "tankColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "tankColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Shorts" %}
                      {%- assign prodNameTitle = "shorts" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "shortsColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "shortsColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Scarf" %}
                      {%- assign prodNameTitle = "scarf" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "scarfColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "scarfColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Dress" %}
                      {%- assign prodNameTitle = "slip dress" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "dressColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "dressColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% endif %}

                  {% if prodNameTitle == "highwaisted_thong" %}
                      {% assign prdImgFileName = "hwthong" %}
                  {% else %}
                      {% assign prdImgFileName = prodNameTitle %}
                  {% endif %}

                  {% assign premImgFileName = prodColorTitle | replace: " ", "" | append: "---" | append: prdImgFileName | append: "---missy---tile_fr.jpg" %}

                  <div class="col velaProBlock quickadd carousel-item ept" data-size-guid="{% render 'collection-selectors-charts', product_target: product %}">
                    <div class="col-wrap velaProBlockInner">
                      {% if hasFlag == "true" %}
                      <div class="prodTileFlag">
                        <div class="prodTileFlagInnerWrapper">
                          <span>{{ prodFlag }}</span>
                        </div>
                      </div>
                      {% endif %}
                      <div class="product-image proHImage d-flex flexJustifyCenter">
                        <a class="proFeaturedImage" href="{{ product.url }}" draggable="false">
                          {% for image in product.images %}
                            {%- assign itemNumber = forloop.index -%}
                            {%- if itemNumber == 1 -%}
                              <img class="lazyload" width="240" height="300" draggable="false" alt="{{ image.alt }}" title="{{ image.alt }}" data-src="{{ image | img_url: 'master' }}">
                            {%- endif -%}
                          {% endfor %}
                          <div class="col-pro-details">
                            {% if product.type contains "Underwear" %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% else %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% endif %}
                            <!-- <div class="prodTileReviewWrapper">
                              {% if product.metafields.stamped.reviews_average %}
                              <span class="colReviewAggrWrapper" draggable="false"><span>{{ product.metafields.stamped.reviews_average | round:2 }}</span><i class="reviewStar"></i></span>
                              {% endif %}
                            </div> -->
                          </div>
                        </a>
                      </div>
                      <div class="product-details proButton clearfix">
                        {% if product.type == "Silk Scarf" %}
                            {%- assign variantCount = product.variants | size -%}
                            <form action="/cart/add" method="post" enctype="multipart/form-data" class="formAddToCart">            				  
                                <input type="hidden" name="id" value="{{ product.variants.first.id }}" />
                                {%- if sold_out -%}
                                    <button class="btn btnProduct btnAddToCart disabled" type="submit" value="Submit" disabled>
                                        <span class="icons icon-handbag"></span>
                                        <span class="text">{{ 'products.product.sold_out' | t }}</span>
                                    </button>
                                {%- else -%}
                                    {%- if variantCount > 1 -%}
                                        <a class="btn btnProduct btnAddToCart" href="{{ product.url }}" title="{{ 'products.product.select_options' | t }}">
                                            <span class="icons icon-handbag"></span>
                                            <span class="select_options text">{{ 'products.product.select_options' | t }}</span>
                                        </a>
                                    {%- else -%}
                                        <div class="quick_btn">
                                          <div class="productQuickAdd">
                                            <button class="btn  btnProduct btnAddToCart" type="submit" value="Submit">
                                              <div class="quickAddBtnInnerWrapper">
                                                <span class="text">Add to Cart</span>
                                                <div class="prodTilePriceWrapper{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price %} vipHighlight{%endif%}">
                                                  <p class="price bfx-price">{{ product.price | money_without_trailing_zeros  }}</p>
                                                  {% if customer.tags contains 'Active Subscriber' %}
                                                  {% if product.type == "Mask"%}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$6</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Core Bralette" or product.type == "Cotton Bralette" or product.type == "Mesh Bralette" %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Wrap Top" or product.type contains "Silk"  %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Core Underwear" and product.tags contains "Featured Style" or product.type == "Mesh Underwear" %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$9</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type == "Core Underwear" or product.type == "Cotton Underwear" or product.type == "Tank"  or product.type == "Bodysuit" or product.type == "Mesh Underwear" %}
                                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', '' }}</div>
                                                  {% else %}
                                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                                  {% endif %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                                    {%- assign discount = product.price | times: 43.24 | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                                    {% comment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                                    {% endcomment %}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$7.50</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% elsif product.type contains "Premium" %}
                
                                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                
                                                  {% elsif product.type contains "Surprise Panty Pack" %}
                
                                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                
                                                  {% endif %}
                
                                                  {% else %}
                                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                                    {%- assign discount = product.price | times: shop.metafields.ebyacf.sitewide_discount_price | divided_by: 100 -%}
                                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                                    {% else %}
                                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                                    {% endif %}
                                                  </div>
                                                  {% endif %}                                
                                                </div>
                                              </div>
                                            </button>
                                          </div>
                                          <span class="quickAddPriceNote{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price != "" %} vipHighlight{%endif%}">subscriber price</span>
                                        </div>
                                    {%- endif -%}
                                {%- endif -%}
                            </form>
                        {% else %}
                            <div class="quick_btn">
                              <div class="productQuickAdd">
                                <button class="btn btnProduct btnProductQuickAdd" data-href="#velaQuickAdd-{{ product.id }}" data-handle="{{ product.handle }}" title="Quick Add +">
                                  <div class="quickAddBtnInnerWrapper">
                                    <span class="text">Add to Cart</span>
                                    <div class="prodTilePriceWrapper{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price %} vipHighlight{%endif%}">
                                      <p class="price bfx-price">{{ product.price | money_without_trailing_zeros  }}</p>
                                      {% if customer.tags contains 'Active Subscriber' %}
                                      {% if product.type == "Mask"%}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                        {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$6</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Core Bralette" or product.type == "Cotton Bralette" or product.type == "Mesh Bralette" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Wrap Top" or product.type contains "Silk"  %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Core Underwear" and product.tags contains "Featured Style" or product.type == "Mesh Underwear" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$9</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type == "Core Underwear" or product.type == "Cotton Underwear" or product.type == "Tank"  or product.type == "Bodysuit" or product.type == "Mesh Underwear" %}
                                      {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', '' }}</div>
                                      {% else %}
                                      <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                      {% endif %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                        {%- assign discount = product.price | times: 43.24 | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                        {% comment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                        {% endcomment %}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$7.50</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                        {% endif %}
                                      </div>
                                      {% elsif product.type contains "Premium" %}
    
                                      <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
    
                                      {% elsif product.type contains "Surprise Panty Pack" %}
    
                                      <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
    
                                      {% endif %}
    
                                      {% else %}
                                      <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                        {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                        {%- assign discount = product.price | times: shop.metafields.ebyacf.sitewide_discount_price | divided_by: 100 -%}
                                        {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                        <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                        {% else %}
                                        <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                        {% endif %}
                                      </div>
                                      {% endif %}                                
                                    </div>
                                  </div>
                                </button>
                                <div class="selection-wrapper" id="velaQuickAdd-{{ product.id }}">
                                </div>
                              </div>
                              <span class="quickAddPriceNote{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price != "" %} vipHighlight{%endif%}">subscriber price</span>
                            </div>
                          {% endif %}
                      </div>
                    </div>
                  </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </article>
  </div>
  <div class="shopTheLookWrapper thirdLook">
    <article class="lookWrapper">
      <div class="lookWrapperCol imgWrapper">
        <img class="lazyload" title="" alt="" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_1440_-_we-silk_-_blush-collection.jpg" width="" height="" />
      </div>
      <div class="lookWrapperCol prodWrapper premium_product_section">
        {% assign collection = collections["washable-eco-silk-shop-the-look-three"] %}
        <div class="product-grd collection_group fullwidth epcWrapper">
          <div class="products_cols collection_group_list ebyContainer pcs">
            {% for product in collection.products limit: block.settings.pdp_range %}
                  {%- assign hasFlag = "false" -%}
                  {% for prodTag in product.tags %}
                      {% if prodTag == "flag-new_arrival" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "New" %}
                      {% endif %}
                      {% if prodTag == "flag-on_sale" %}
                          {% assign hasFlag = "true" %}
                          {% assign prodFlag = "Sale" %}
                      {% endif %}
                  {% endfor %}
                  {% if product.type == "Silk Blouse" %}
                      {%- assign prodNameTitle = "blouse" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "blouseColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "blouseColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Tank" %}
                      {%- assign prodNameTitle = "tank" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "tankColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "tankColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Shorts" %}
                      {%- assign prodNameTitle = "shorts" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "shortsColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "shortsColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Scarf" %}
                      {%- assign prodNameTitle = "scarf" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "scarfColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "scarfColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% elsif product.type == "Silk Dress" %}
                      {%- assign prodNameTitle = "slip dress" -%}
                      {%- assign prodFabricTitle = "washable eco silk" -%}
                      {% for prodTag in product.tags %}
                          {% if prodTag contains "dressColor-" %}
                              {%- assign prodColorTitle = prodTag | replace: "dressColor-", "" | replace: "_", " " -%}
                          {% endif %}
                      {% endfor %}
                  {% endif %}

                  {% if prodNameTitle == "highwaisted_thong" %}
                      {% assign prdImgFileName = "hwthong" %}
                  {% else %}
                      {% assign prdImgFileName = prodNameTitle %}
                  {% endif %}

                  {% assign premImgFileName = prodColorTitle | replace: " ", "" | append: "---" | append: prdImgFileName | append: "---missy---tile_fr.jpg" %}

                  <div class="col velaProBlock quickadd carousel-item ept" data-size-guid="{% render 'collection-selectors-charts', product_target: product %}">
                    <div class="col-wrap velaProBlockInner">
                      {% if hasFlag == "true" %}
                      <div class="prodTileFlag">
                        <div class="prodTileFlagInnerWrapper">
                          <span>{{ prodFlag }}</span>
                        </div>
                      </div>
                      {% endif %}
                      <div class="product-image proHImage d-flex flexJustifyCenter">
                        <a class="proFeaturedImage" href="{{ product.url }}" draggable="false">
                          {% for image in product.images %}
                            {%- assign itemNumber = forloop.index -%}
                            {%- if itemNumber == 1 -%}
                              <img class="lazyload" width="240" height="300" draggable="false" alt="{{ image.alt }}" title="{{ image.alt }}" data-src="{{ image | img_url: 'master' }}">
                            {%- endif -%}
                          {% endfor %}
                          <div class="col-pro-details">
                            {% if product.type contains "Underwear" %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% else %}
                            <h3 href="{{ product.url }}" draggable="false">
                              <span>{{ prodFabricTitle| capitalize  }}</span>
                              <span>{{ prodNameTitle | capitalize  }}</span>
                              <span class="small">{{ prodColorTitle | capitalize  }}</span>
                            </h3>
                            {% endif %}
                            <!-- <div class="prodTileReviewWrapper">
                              {% if product.metafields.stamped.reviews_average %}
                              <span class="colReviewAggrWrapper" draggable="false"><span>{{ product.metafields.stamped.reviews_average | round:2 }}</span><i class="reviewStar"></i></span>
                              {% endif %}
                            </div> -->
                          </div>
                        </a>
                      </div>
                      <div class="product-details proButton clearfix">
                        <div class="quick_btn">
                          <div class="productQuickAdd">
                            <button class="btn btnProduct btnProductQuickAdd" data-href="#velaQuickAdd-{{ product.id }}" data-handle="{{ product.handle }}" title="Quick Add +">
                              <div class="quickAddBtnInnerWrapper">
                                <span class="text">Add to Cart</span>
                                <div class="prodTilePriceWrapper{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price %} vipHighlight{%endif%}">
                                  <p class="price bfx-price">{{ product.price | money_without_trailing_zeros  }}</p>
                                  {% if customer.tags contains 'Active Subscriber' %}
                                  {% if product.type == "Mask"%}
                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                  {% else %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                  {% endif %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                    {% comment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                    {% endcomment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$6</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                    {% endif %}
                                  </div>
                                  {% elsif product.type == "Core Bralette" or product.type == "Cotton Bralette" or product.type == "Mesh Bralette" %}
                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                  {% else %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                  {% endif %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                    {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                    {% comment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                    {% endcomment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                    {% endif %}
                                  </div>
                                  {% elsif product.type == "Wrap Top" or product.type contains "Silk"  %}
                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                  {% else %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                  {% endif %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                    {%- assign discount = product.price | times: 42.27 | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                    {% comment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                    {% endcomment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$20</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                    {% endif %}
                                  </div>
                                  {% elsif product.type == "Core Underwear" and product.tags contains "Featured Style" or product.type == "Mesh Underwear" %}
                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', ''  }}</div>
                                  {% else %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                  {% endif %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                    {%- assign discount = product.price | times: 44 | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_currency -%}
                                    {% comment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                    {% endcomment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$9</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )"}}</p>
                                    {% endif %}
                                  </div>
                                  {% elsif product.type == "Core Underwear" or product.type == "Cotton Underwear" or product.type == "Tank"  or product.type == "Bodysuit" or product.type == "Mesh Underwear" %}
                                  {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.metafields.ebyacf.vip_product_price | replace: '/ ', '' | replace: ' members', '' }}</div>
                                  {% else %}
                                  <div class="priceProduct {% if customer.tags contains 'Active Subscriber' %}vipHighlight {%endif%}{% if on_sale %}priceSale bfx-sale-price{% endif %}bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>
                                  {% endif %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %} 
                                    {%- assign discount = product.price | times: 43.24 | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                    {% comment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }}% Off</span></p>
                                    {% endcomment %}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">$7.50</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                    {% endif %}
                                  </div>
                                  {% elsif product.type contains "Premium" %}

                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>

                                  {% elsif product.type contains "Surprise Panty Pack" %}

                                  <div class="priceProduct {% if on_sale %}priceSale bfx-sale-price{% endif %}{% if product.metafields.ebyacf.evergreen_price_test %} st-tangentPrice{%endif%} bfx-price">{{ product.price | money_without_trailing_zeros  }}</div>

                                  {% endif %}

                                  {% else %}
                                  <div class="ebyProdTile-vipPriceWrapper bfx-remove-element">
                                    {% if shop.metafields.ebyacf.enable_holiday_mode-vip_pg or shop.metafields.ebyacf.enable_holiday_mode-coll_pg %}
                                    {%- assign discount = product.price | times: shop.metafields.ebyacf.sitewide_discount_price | divided_by: 100 -%}
                                    {%- assign holidayPrice = product.price | minus: discount | money_without_trailing_zeros  -%}
                                    <p>/ <span class="ebyHolidayPrice bfx-sale-price bfx-price">${{holidayPrice}}</span> <span class="ebyHolidayDiscount bfx-price">{{ shop.metafields.ebyacf.sitewide_discount_price }} Off</span></p>
                                    {% else %}
                                    <p class="bfx-price">{{product.metafields.ebyacf.vip_product_price | replace: "( " | replace: " members )" }}</p>
                                    {% endif %}
                                  </div>
                                  {% endif %}                                
                                </div>
                              </div>
                            </button>
                            <div class="selection-wrapper" id="velaQuickAdd-{{ product.id }}">
                            </div>
                          </div>
                          <span class="quickAddPriceNote{% if customer.tags contains 'Active Subscriber' and product.metafields.ebyacf.vip_product_price != "" %} vipHighlight{%endif%}">subscriber price</span>
                        </div>
                      </div>
                    </div>
                  </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </article>
  </div>
</section>

<section class="inspirationContentContainer ss-inspiration_block">
  <article class="insoWrapper">
     <img class="desktop_only lazyload" title="" alt="" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_1440_-_we-silk_-_inside-outside_31b74c81-c0c8-4d58-8b8d-656fe43e2e14.jpg" width="" height="" />
     <img class="mobile_only lazyload" title="" alt="" data-src="https://cdn.shopify.com/s/files/1/0313/4062/5964/files/LP_-_376_-_we-silk_-_inside-outside_78719565-592c-4280-a2f8-446bd96b67ad.jpg" width="" height="" />
     <h3>Washable eco silk is a collection of transitional garments that’ll take you from the couch to the office, out to dinner and drinks, and back in a super-chic look that’s luxuriously comfortable.</h3>
  </article>
</section>

{% schema %}
  {
    "name": "Collection",
    "settings": [
	],
	"blocks": [
      {
        "type": "tab",
        "name": "Collection",
        "settings": [
		  {
            "type": "collection",
            "id": "collection",
            "label": "Collection"
          },
		  {
            "type": "text",
            "id": "sub_title",
            "label": "Sub Title"
          },
		  {
            "type": "text",
            "id": "title",
            "label": "Title"
          },
		  {
            "type": "text",
            "id": "caption",
            "label": "Caption"
          },
          {
            "type": "range",
            "id": "pdp_range",
            "min": 2,
            "max": 50,
            "step": 1,
            "label": "Show Product",
            "default": 8
          }
		]
	  }
	]
  }
{% endschema %}