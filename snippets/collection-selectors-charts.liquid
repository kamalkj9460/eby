{%- case product_target.type -%}
  {%- when 'Mask' -%}
    {%- assign size_guid_product = ' maskChart' -%}
  {%- when 'Mask Pack' -%}
    {%- assign size_guid_product = ' maskChart' -%}
  {%- when 'Panty Pack' -%}
    {%- assign size_guid_product = ' pantyChart' -%}
  {%- when 'Core Underwear' or 'Premium Underwear' or 'Mesh Underwear' -%}
    {%- assign size_guid_product = ' pantyChart' -%}
  {%- when 'Cotton Underwear' -%}
    {%- assign size_guid_product = ' pantyChart' -%}
  {%- when 'Surprise Panty Pack' -%}
    {%- assign size_guid_product = ' pantyChart' -%}
  {%- when 'Core Bralette' or 'Premium Bralette' or 'Mesh Bralette' -%}
    {%- assign size_guid_product = ' braletteChart' -%}
  {%- when 'Cotton Bralette' -%}
    {%- assign size_guid_product = ' braletteChart' -%}
  {%- when 'Bodysuit' -%}
    {%- assign size_guid_product = ' bodysuitChart' -%}
  {%- when 'Tank' -%}
    {%- assign size_guid_product = ' tankChart' -%}
  {%- when 'Emissary Tank' -%}
    {%- assign size_guid_product = ' tankChart' -%}
  {%- when 'Bralette Panty Set' -%}
    {%- assign size_guid_product = ' pantyChart braletteChart' -%}
  {%- when 'Bra & Panty Set' -%}
    {%- assign size_guid_product = ' pantyChart braletteChart' -%}
  {%- when 'Wrap Top' -%}
    {%- assign size_guid_product = ' wrapTopChart' -%}
{%- endcase -%}
{{ size_guid_product }}